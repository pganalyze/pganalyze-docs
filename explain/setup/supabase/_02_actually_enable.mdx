On Supabase, `shared_preload_libraries` is not user-configurable, but it includes `auto_explain`
by default. If `auto_explain` doesn't show up in `shared_preload_libraries` for your project, contact Supabase support.

Once that's confirmed, continue to the next step to configure `auto_explain` at the role level.
